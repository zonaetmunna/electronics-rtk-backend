const USER_ROLE = {
  superAdmin: 'superAdmin',
  customer: 'customer',
  manager: 'manager',
  admin: 'admin',
}

const UserStatus = ['in-progress', 'blocked']

module.exports = { USER_ROLE, UserStatus }
